models.py変更後、
    python manage.py makemigrations
    python manage.py migrate
を行うことにより変更がデータベースに反映される

データベースをいじるときは
    python manage.py shell
からオブジェクトの作成などを行う

## オブジェクト作成方法 ##
1. MODEL_NAME.objects.create(kwargs)
2. obj = MODEL_NAME(kwargs)
   obj.save()

## オブジェクトの削除 ##
   obj.delete()

## 画面表示 ##

   python3 manage.py runserver 0:8000

web browserで
   http://127.0.0.1:10000/worker_list/
vagrantの設定ファイルでローカルホストのport10000を仮想ホストのport8000にport forwardingしている
